name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Show files
        run: ls -la

      - name: Simulate build
        run: echo "Building app..."

      - name: Simulate test
        run: echo "Running tests..."